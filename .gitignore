.build/
build/
*.xcodeproj
.DS_Store

# Local signing config (personal Apple Development identity).
scripts/signing.env

# Personal notarized-release / App Store Connect workflow (me-only; not part of
# the public source).
scripts/asc.env
scripts/setup-asc.sh
scripts/notarize-release.sh
scripts/build-dmg.sh

# Local Claude Code tooling state and instructions (not part of the public source).
.claude/
CLAUDE.md

# Cached localization-review artifacts (working material, kept out of /tmp;
# outcomes live in the .strings files + docs/localization-review-backlog.md).
.localization-reviews/

# Python virtualenvs created by uv for the helper scripts (the uv.lock IS tracked).
.venv/
scripts/.venv/
